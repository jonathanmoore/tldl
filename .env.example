# Transport mode: "stdio" (default, recommended) or "http".
# - stdio: Claude Code spawns the process, no auth, no port. No env vars
#   required.
# - http: uvicorn + bearer token. Not used by the setup flow today;
#   reserved for a future home-server path. Cloud hosting is not supported.
TLDL_TRANSPORT=stdio

# Required when TLDL_TRANSPORT=http. Not needed for local stdio.
# Bearer token clients must send as `Authorization: Bearer <token>`.
# Generate with: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
MCP_BEARER_TOKEN=

# Optional: server log level. Default INFO.
LOG_LEVEL=INFO

# Optional: HTTP listen port (only used in http mode). Default 8000.
PORT=8000
